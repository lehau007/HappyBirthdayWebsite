# Copy this file to .env and fill in the values.
# VITE_API_BASE_URL is baked into the JS bundle at build time.
# For local development it should point to the backend running on your machine.
# For Docker Compose it is overridden via the `args` block in docker-compose.yml.
VITE_API_BASE_URL=http://localhost:8000
